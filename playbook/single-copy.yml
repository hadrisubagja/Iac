---
- hosts: master
  become: yes
  gather_facts: no

  tasks:
   - name: chmod
     shell: chown -R mysql:mysql /mnt/mysql/data/
     args:
       executable: /bin/bash
    #  with_items:
      
    #   - chmod --reference=/var/lib/mysql /mnt/wikiceria/mysql/data/
  