---
# tasks file for roles/mysql

- name: Install Dependency
  ackage: name={{ item }} state=present update_cache=yes
  with_items:
   - systemd
   - man-db

#Download package config
- name: Download MySQL Cluster Repository
  get_url:
    url: https://downloads.mysql.com/archives/get/p/14/file/mysql-cluster-community-server_7.6.6-1ubuntu18.04_amd64.deb
    dest: /home/hadrisubagja/mysql-cluster-community-management-server_7.6.19-1ubuntu18.04_amd64.deb

# Update repository
- name: Update repo
  shell: apt update
  # register: apt_update
  
# - name: dump update
#   debug:
#     msg: "{{ apt_update }}"

# - name: install data note binary
#   shell: dpkg -i mysql-cluster-community-server_7.6.6-1ubuntu18.04_amd64.deb


# - name: Installing MySQL & Dependencies
#   package: name={{ item }} state=present update_cache=yes
#   with_items:
#    - python-pip
#    - python-virtualenv
#    - python-pymysql
#    - python-mysqldb
#    - libmysqlclient-dev

# - name: Add PGP key
#   apt_key:
#     keyserver: hkp://pgp.mit.edu:80
#     id: 5072E1F5
#   # state: present

# - name: Installing MySQL & Dependencies
#   package: name={{ item }} state=present update_cache=yes
#   with_items:
#    - mysql-common
#    - mysql-server
#    - mysql-client
#    - mysql_secure_installation
 

# #Start Service
# - name: start and enable mysql service
#   service:
#     name: mysql
#     state: started
#     enabled: yes

#Create User Database    
# - name: creating mysql user (medium_post)
#   mysql_user:
#     name: "{{db_user}}"
#     password: "{{db_pass}}"
#     priv: '*.*:ALL'
#     host: '%'
#     state: present

# - name: creating medium_db
#   mysql_db:
#     name: "{{db_name}}"
#     state: present

# - name: Enable remote login to mysql
#   lineinfile:
#     path: /etc/mysql/mysql.conf.d/mysqld.cnf
#     regexp: '^bind-address'
#     line: 'bind-address = 0.0.0.0'
#     backup: yes
#   notify:
#    - Restart mysql
