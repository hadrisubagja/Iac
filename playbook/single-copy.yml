---
- hosts: master
  become: yes
  gather_facts: no

  tasks:
   - name: chmod
     shell: chown --reference=/var/lib/mysql /mnt/wikiceria/mysql/data/
    #  with_items:
      
    #   - chmod --reference=/var/lib/mysql /mnt/wikiceria/mysql/data/
  